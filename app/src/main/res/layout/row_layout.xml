<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <ImageView
        android:id="@+id/company_poster"
        android:layout_width="100dp"
        android:layout_height="75dp"
        android:layout_alignParentLeft="true"
        android:src="@drawable/mlab"/>
    <TextView
        android:id="@+id/company_title"
        android:layout_width="175dp"
        android:layout_height="75dp"
        android:layout_toRightOf="@+id/company_poster"
        android:text="This is a company name"
        android:gravity="center"
        android:textColor="#FFFFFF"/>

    <TextView
        android:id="@+id/company_rating"
        android:layout_width="wrap_content"
        android:layout_height="75dp"
        android:text="This is a rating"
        android:layout_toRightOf="@id/company_title"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:layout_alignParentRight="true"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="#FFFFFF"
        android:layout_below="@id/company_poster" >

    </View>

</RelativeLayout>