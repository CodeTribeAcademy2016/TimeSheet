<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.com.timesheet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".ProfileActivity"
            android:label="Profile"
            />

        <!--
        whats happening here?
        <activity android:name=".checkin" />
        -->
        <activity
            android:name=".employee.InternHomeActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".employee.CheckOutActivity"
            android:label="Check Out"
            />
        <activity android:name=".employee.CheckInActivity"
            android:label="Check In"/>
        <activity android:name=".employee.ApplyLeaveActivity"
            android:label="Apply Leave"
            />
        <activity android:name=".employee.EventActivity"
            android:label="Events"
            />
        <activity android:name=".supervisor.AddEmployeeActivity"
            android:label="Add Employee"
            />
        <activity
            android:name=".supervisor.EmployeeList"
            android:label="@string/title_activity_employee_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".supervisor.CustomRow" />
        <activity
            android:name=".supervisor.Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".supervisor.LeaveForm"
            android:label="LeaveForm"
            />
        <activity
            android:name=".skillscordinator.SkillsCoordinator"
            android:label="@string/title_activity_skills_coordinator"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".skillscordinator.CompanyList"
            android:label="Company List"
            />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
    </application>

</manifest>
